# manual-vm-setup
This project showcases a manual setup of a multi-tier Java web application using multiple virtual machines. Each service runs on a separate VM and is configured manually to replicate a real-world infrastructure, focusing on service dependencies, networking, and application deployment using Vagrant.
